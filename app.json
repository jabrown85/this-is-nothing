{
  "name": "this-is-nothing",
  "scripts": {
  },
  "env": {
    "LANG": {
      "required": true
    },
    "RACK_ENV": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "addons": [

  ],
  "scripts": {
    "test": "rspec -f RspecTap::Formatter"
  },
  "buildpacks": [
    { "url": "https://github.com/heroku/heroku-buildpack-ci-redis" },
    { "url": "https://github.com/heroku/heroku-buildpack-ci-postgresql" },
    { "url": "heroku/ruby" }
  ]
}
