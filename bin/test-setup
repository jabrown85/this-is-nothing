#!/bin/bash

if [ "$1" = "4" ]; then
  echo "This one is going to run out of disk space!"
  for i in {1..50}
  do
    echo "creating large file for the $i time"
    fallocate -l 50G "file.$1"
  done
  exit 0
else
  echo "This one is going to run fine"
  exit 0
fi